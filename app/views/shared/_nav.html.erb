<nav class="navbar navbar-noodles" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header col-md-1">
      <a class="navbar-brand" href="/"><%= t('general.noodles') %></a>
    </div>

    <div class="col-md-8 col-md-offset-1">

      <ul class="nav navbar-nav">

        <li class="<%= nav_active_class(:recipes) %>">
          <%= link_to :recipes do %>
            <span class="geomicon geomicon-rows"></span>
            <span class="title"><%= t('general.recipes') %></span>
          <% end %>
        </li>

      </ul>

    </div>

    <ul class="nav navbar-nav navbar-right">

      <% if user_signed_in? %>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">

            <%= image_tag current_user.gravatar_url(:rating => 'R', :secure => true, :size => 60, :default => 'identicon'), class: 'avatar' %>
            <span class="caret"></span></a>

            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to t('general.settings'), :edit_user_registration %>
              </li>
              <li class="divider"></li>
              <li>
                <%= link_to t('general.sign_out'), :destroy_user_session %>
              </li>
            </ul>

          </a>
        </li>

      <% else %>

        <li>
          <%= link_to t('general.sign_in'), :create_user_session %>
        </li>

      <% end %>

    </div>
  </div>
</nav>
