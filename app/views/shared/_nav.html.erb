<nav class="navbar navbar-noodles" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Noodles</a>
    </div>
    <ul class="nav navbar-nav navbar-right">

      <% if user_signed_in? %>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">

            <%= image_tag current_user.gravatar_url(:rating => 'R', :secure => true, :size => 60, :default => 'identicon'), class: 'avatar' %>
            <span class="caret"></span></a>

            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to t('settings'), :edit_user_registration %>
              <li>
              <li class="divider"></li>
              <li>
                <%= link_to 'Sign Out', :destroy_user_session %>
              </li>
            </ul>

          </a>
        </li>

      <% else %>

        <li>
          <%= link_to 'Sign In', :create_user_session %>
        </li>

      <% end %>

    </div>
  </div>
</nav>
