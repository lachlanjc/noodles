<% unless Rails.env.development? %>
  <script>
  !function(g,s,q,r,d){r=g[r]=g[r]||function(){(r.q=r.q||[]).push(arguments)};
  d=s.createElement(q);q=s.getElementsByTagName(q)[0];
  d.src='//d1l6p2sc9645hc.cloudfront.net/tracker.js';q.parentNode.insertBefore(d,q)}(window, document, 'script', '_gs');
  _gs('GSN-107564-I');
  </script>
  <%= heap_analytics %>
  <% if user_signed_in? %>
    <%= intercom_script_tag({
      app_id: ENV['INTERCOM_APP_ID'],
      user_id: current_user.id,
      email: current_user.email,
      name: current_user.first_name,
      custom_data: {
        recipes_count: user_recipe_count,
        shared_recipes_count: user_shared_recipe_count,
        collections_count: user_collection_count,
        referring_url: current_user.referring_url,
        landing_url: current_user.landing_url
      }
    }) %>
    <%= heap_identify(current_user.email, name: current_user.first_name) %>
  <% end %>
<% end %>
