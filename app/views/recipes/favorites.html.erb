<% activate_nav!(:favorites) %>

<%= render 'shared/nav' %>

<% if current_user and @recipes.count == 0 %>

  <div class="container col-8">

    <div class="panel-title">
      <%= t('.no_favorites_yet') %>
    </div>
    <div class="panel-body">
      <%= t('.favorites_are') %>
      <br>
      <%= t('.to_add_favorites') %>
      <br>
      <%= link_to t('.back_to_r'), recipes_path, class: 'btn btn-primary' %>
    </div>

  </div>

<% else %>

  <div class="page container col-8 col-offset-2 recipes-list-container print-full-width">

    <h1 class="page-title printer-copy">
      <%= t('.all_recipes') %>
    </h1>

    <div class="list-group recipes-list-group">

      <% @recipes.each do |recipe| %>

        <%= link_to (recipe) do %>
          <div class="list-group-item">

            <% if recipe.favorite == true %>
              <div class="pull-right">
                <span class="geomicon geomicon-star text-yellow"></span>
              </div>
            <% end %>

            <h3><%= recipe.title %></h3>

            <% unless recipe.description.length == 0 %>
              <p><%= recipe.description %></p>
            <% end %>

          </div>
        <% end %>

      <% end %>

    </div>

    <p class="printer-copy">
      <%= t('recipes.printing.print_copy') %>
    </p>

  </div>

<% end %>

<div class="container col-8 col-offset-2 footer">
  <%= render 'shared/footer' %>
</div>


<%= link_to :new_recipe, class: 'btn-paper btn-paper-right animated bounceInUp' do %>
  <span class="geomicon geomicon-plus"></span>
<% end %>
