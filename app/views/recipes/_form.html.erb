<%= form_for(@recipe) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title, autofocus: true, class: 'text-input' %>

  <%= f.label :description %>
  <%= f.text_area :description, class: 'text-input js-auto-size' %>

  <div class="well">
    <% if @recipe.img == nil %>
      <h2>Add a photo</h2>
    <% else %>
      <h2>Change this recipe's photo</h2>
    <% end %>
    <%= f.file_field :img %>
    <% if nav_active?(:edit_recipe) && @recipe.img == '' %>
      <span>or </span><%= link_to 'Remove current image', remove_image_path(@recipe), class: 'btn btn-sm btn-default in-text' %>
      <br>
    <% end %>
  </div>

  <%= f.label :ingredients %>
  <%= f.text_area :ingredients, class: 'text-input js-auto-size' %>

  <%= f.label :instructions %>
  <%= f.text_area :instructions, class: 'text-input js-auto-size' %>

  <div class="action-list">

    <a class="item-favorite">
      <label class="control">
        <%= f.check_box :favorite %>
        <div class="control-indicator">
          <%= inline_svg('ios7-star-outline.svg') %>
        </div>
        <div class="control-indicator-checked">
          <%= inline_svg('ios7-star.svg') %>
        </div>
        <span><%= t('general.favorite') %></span>
      </label>
    </a>

    <% if nav_active?(:edit_recipe) %>
      <%= link_to @recipe, method: :delete, data: { confirm: t('general.delete_forever') }, class: 'item-danger' do %>
        <%= inline_svg('ios7-trash-outline.svg') %>
        <span><%= t('general.delete') %></span>
      <% end %>
    <% end %>

  </div>

  <div class="row">
    <% if nav_active?(:new_recipe) %>
      <%= f.submit t('general.add_recipe'), class: 'btn btn-primary' %>
    <% else %>
      <%= f.submit t('general.save'), class: 'btn btn-primary' %>
    <% end %>
  </div>

<% end %>
