<%= form_for(@recipe) do |f| %>
  <%= f.label :notes, 'Anything you want to remember for next time?', class: 'm1-top' %>
  <%= f.text_area :notes, class: 'text-input panel full-width m0 notes js-auto-size' %>
  <%= f.submit t('general.save'), class: 'btn-success btn-sm m1-btm center' %>
<% end %>
